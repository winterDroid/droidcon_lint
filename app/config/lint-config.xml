<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <issue id="InvalidPackage" severity="ignore" />

    <!-- Here we increase the severity to error as an example -->
    <issue id="LogNotTimber" severity="error" />
</lint>
